# API Configuration
# URL of the backend API. 
# If not set, defaults to "/api" which uses the Vite proxy in development.
# For production, set this to the full URL of your API (e.g., https://api.handoverkey.com/api/)
VITE_API_URL=
